//Handling Files in C
#include<stdio.h>
#include<cstring>
int main(){
FILE* fptr;
/*if(fptr!=NULL){
   fptr=fopen("hey.txt","w");
   fputs ("Hello!\n",fptr);
   fputs ("This is the way you create a file using C languange...\n",fptr);
   fclose(fptr);
}
else{printf("Couldn't access the file!)";}*/

/*if(fptr!=NULL){
   fptr=fopen("hey.txt","a");
   fputs ("And this is the way you appaned to a file using C languange...",fptr);
   fclose(fptr);
}
else{printf("Couldn't access the file!)";}*/

/*if(fptr!=NULL){
        fptr=fopen("hey.txt","r");
        char content[10000];
while(fgets(content , 10000 , fptr)){
    printf("%s",content);
}
   fclose(fptr);
}
else{printf("Couldn't access the file!)";}*/
}
